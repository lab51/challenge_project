{% extends 'foundation_base.html.twig' %}
{% block body %}
    <div class="large-9 columns borders-all">
        <ul class="breadcrumbs">
            <li><a href="/">home</a></li>
            <li><a href="{{ path('lab51_challenge_homepage') }}">my challenges</a></li>
            <li class="current">add challenge</li>
        </ul>
        <h3>Inbox</h3>
        <p>We'd love to hear from you. You can either reach out to us as a whole and one of our awesome team members will get back to you, or if you have a specific question reach out to one of our staff. We love getting email all day <em>all day</em>.</p>
        <div class="section-container tabs" data-section>
            <section class="section">
                <div class="content" data-slug="panel1">
<ul>
        {% for msg in messages %}
            <li><a href="{{ path('lab51_inbox_show_message', {'message_id': msg.id}) }}">{{ msg.content|slice(0,90) }}</a> <a href="{{ path('lab51_inbox_delete_message', {'message_id': msg.id}) }}">delete</a></li>
        {% endfor %}   
</ul>
                </div>
            </section>

        </div>
    </div>


{% endblock %}

